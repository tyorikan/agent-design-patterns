# 2026-05-27 作業ログ

## PGE パイプライン品質改善

### リグレッションガード実装
- verdict 判定ロジックに「前回比スコア低下時の REVISE 強制」を追加
- 6段階の verdict 判定: 最大反復→ブロッカー→リグレッション→閾値→改善停滞→LLM verdict
- 改善停滞ルールのバグ修正: `improvement >= 0` チェック削除、`not is_regression` 条件に統一

### Generator セルフチェック
- `allow_commands=True` に変更し、Generator に ruff check / pytest の自律実行を許可
- プロンプトに品質セルフチェック手順を追加（ruff check → 修正 → pytest → 修正 → submit）
- `ruff check --fix` は使用禁止（手動修正を強制）
- import 順のコーディング規約を追加（I001 対策）
- 結果: 初回で score=100、ruff 0 errors を達成。実行時間 22分→6分に短縮

### dynamic output_dir 対応
- `OUTPUT_DIR` をグローバル固定値から `_resolve_output_dir(tool_context)` による動的解決に変更
- `state["output_dir"]` で任意のディレクトリを対象にできるように
- Planner に既存プロジェクト改修モードを追加（初回で既存ファイル一覧を渡す）
- 後方互換: `OUTPUT_DIR = DEFAULT_OUTPUT_DIR` でテスト等からの直接参照を維持

### adk run 対応
- パッケージ内インポートを相対インポート（`from .tools import ...`）に変更
- `__init__.py` でプロジェクトルートを `sys.path` に追加し `shared` パッケージを解決
- conftest.py の `load_pattern_agent()` を `importlib.import_module()` に変更
- `adk run patterns/agentic_pipeline --state '{"output_dir": "..."}' "要件"` で実行可能に

### ドキュメント包括的アップデート
- 3並列監査（秘匿情報・IAC残留・ドキュメント整合性）を実施
- .gitignore: terraform/ 削除、.ruff_cache/ .deepeval/ key.json 追加
- README.md: IaC 参照を修正、技術スタックを BaseAgent + Antigravity SDK に更新
- .gemini/ 配下のドキュメントを最新の実装に追従
- SKILL.md / スキル README を更新
