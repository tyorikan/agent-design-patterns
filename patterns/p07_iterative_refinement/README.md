# Lv.7 Iterative Refinement Pattern

## 概念

**Iterative Refinement** は、1 つのエージェントが**自己評価スコア**を持ち、
閾値を超えるまで自律的に改善を繰り返すパターン。
Review & Critique（Lv.6）と異なり、外部の批評者を持たず**自己完結**で品質を高める。

```
Input
  │
  ▼
┌──────────────────────────────────────────┐
│  Workflow: doc_refinement (条件付きサイクル) │
│                                          │
│  edges=[('START', doc_refiner,           │
│          {'REVISE': doc_refiner})]       │
│                                          │
│  ┌────────────────────────────┐          │
│  │      doc_refiner (LLM)     │          │
│  │                            │          │
│  │  1. コンテンツを生成/改善  │          │
│  │  2. 自己評価（4 項目採点） │          │
│  │  3. スコア判定             │          │
│  │     ├ < 85 → 'REVISE'     │          │
│  │     └ >= 85 → END         │          │
│  └────────────────────────────┘          │
│         ↑ 条件付きサイクル ↓              │
└──────────────────────────────────────────┘
  │
  ▼
Output（品質保証済みドキュメント）
```

## いつ使うか

✅ **適している場合:**
- 品質基準が定量的に定義できる（スコアリング可能な）タスク
- 1 つのエージェントで生成と評価の両方が可能な場合
- 人間の介入なしに自律的に品質を高めたい場合
- コンテンツ生成、コード生成、翻訳品質の向上など

❌ **限界:**
- 自己評価のバイアス（自分に甘くなりがち / スコアがインフレする）
- 多角的な視点が必要なタスクには不向き（→ Lv.6 Review & Critique を検討）
- 改善が収束しない場合に `max_iterations` で強制終了される

## Review & Critique (Lv.6) との違い

| 観点 | Review & Critique (Lv.6) | Iterative Refinement (Lv.7) |
|---|---|---|
| エージェント数 | 2（Generator + Critic） | 1（自己完結） |
| フィードバック源 | 外部 Critic エージェント | 自己評価スコア |
| 停止条件 | Critic の承認 | 定量スコア ≥ 閾値 |
| 多様性 | 🟢 高（異なる視点） | 🟡 中（同一モデルの自己改善） |
| コスト | 🟡 中（2 エージェント分） | 🟢 低（1 エージェント分） |

## このデモのユースケース

**Google Cloud 技術ドキュメント自動生成**

Cloud Pub/Sub などのテーマを指定すると、エージェントが技術ドキュメントを生成し、
4 つの評価基準（技術的正確性 30 点 / 網羅性 25 点 / 読みやすさ 25 点 / 実用性 20 点）で
自己採点しながら、85 点以上になるまで最大 5 回改善を繰り返す。

## アーキテクチャ

```
User: "doc_topic: Pub/Sub を使ったイベント駆動アーキテクチャ"
  │
  ▼
┌────────────────────────────────────────────────┐
│  Workflow: doc_refinement (条件付きサイクル)     │
│  edges=[('START', doc_refiner,                 │
│          {'REVISE': doc_refiner})]             │
│                                                │
│  ┌────────────────────────────────────────┐    │
│  │  LlmAgent: doc_refiner                 │    │
│  │                                        │    │
│  │  instruction:                          │    │
│  │   - 技術ドキュメント作成・改善         │    │
│  │   - 構成: 概要/機能/ユースケース/     │    │
│  │          始め方(コード例)/まとめ        │    │
│  │   - ASCII アート図を含める             │    │
│  │                                        │    │
│  │  自己評価基準:                         │    │
│  │   - 技術的正確性   30 点               │    │
│  │   - 網羅性         25 点               │    │
│  │   - 読みやすさ     25 点               │    │
│  │   - 実用性         20 点               │    │
│  │                                        │    │
│  │  output_key: previous_doc_and_score    │    │
│  │  → 次イテレーションで自身が参照       │    │
│  └────────────────────────────────────────┘    │
│                                                │
│  判定: スコア >= 85 → END（Workflow 終了）      │
│        スコア <  85 → 'REVISE' で改善継続       │
└────────────────────────────────────────────────┘
```

## トレードオフ

| 観点 | 評価 |
|---|---|
| コスト | 🟡 中（反復回数 × 1 エージェント分の LLM 呼び出し） |
| レイテンシ | 🟡 中〜高（最大 5 回ループ） |
| 複雑性 | 🟢 低（Workflow 条件付きサイクル + LlmAgent 1 つ） |
| 品質 | 🟢 定量的な品質保証（スコアベース） |
| 柔軟性 | 🟡 中（評価基準の変更は instruction の修正で対応） |
| 自律性 | 🟢 高（人間の介入なしに自律的に改善） |

## 実行方法

```bash
PYTHONPATH=../.. python3 demo.py
```

## 学習ポイント

1. **`Workflow` の条件付きサイクル** — `{'REVISE': doc_refiner}` の dict エッジで条件付きループを構成。v2 では無条件サイクルは禁止され、必ず条件分岐が必要
2. **自己評価スコアによる停止制御** — スコア ≥ 85 なら END（暗黙の終了）、< 85 なら `'REVISE'` を返して同じノードに再入する仕組み
3. **`output_key` の再帰的活用** — 前のイテレーションの出力を `previous_doc_and_score` として次のイテレーションの入力に渡す
4. **定量的品質基準の設計** — 4 項目 100 点満点の採点ルーブリックを instruction に明示することで、改善の方向性をガイド
5. **自己評価の限界** — 同一モデルによる自己評価はスコアインフレの傾向があるため、閾値と最大反復回数の両方で制御する

## 次のステップ

→ **[Lv.8 Coordinator Pattern](../p08_coordinator/)**: LLM による動的ルーティング
